#include <iostream>
#include "cycle.h"
using namespace std;
using namespace  N;

int main() {
    cycle _obj;
    _obj.welcome();



    return 0;
}

//transition record (buy records to save for admin)-> loading process
//user data to store in file and loading process (id /username /pass / amount)
//buying number -> if already bought,cannot buy again
//to show number to buy -> loading